name = "01_ingest"
description = "Ingest and clean FASTA sequences"
inputs = ["sample_peptides.fasta"]
outputs = ["outputs/cleaned_sequences.csv"]

[container]
image = "python:3.11-slim"

[scripts]
run = "run.sh"
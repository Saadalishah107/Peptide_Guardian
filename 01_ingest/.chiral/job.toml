name = "01_ingest"
description = "Ingest and clean FASTA sequences"
outputs = ["outputs/cleaned_sequences.csv", "outputs/dataset_summary.json"]

[container]
image = "python:3.11-slim"

[scripts]
run = "run.sh"

[storage]
# This tells Silva to preserve this folder after the container stops
outputs = "outputs"
name = "b_features"
description = "Phase 2: Screen"
# Use the folder name of Node 1
depends_on = ["a_ingest"] 
# Silva looks for this specific file inside the 'a_ingest' results
inputs = ["cleaned_sequences.csv"] 
outputs = ["results/peptide_features.csv", "results/screening_report.html"]

[container]
image = "chiral-guardian:v1"

[scripts]
run = "run.sh"
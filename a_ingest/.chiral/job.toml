name = "a_ingest"
description = "Validates input FASTA files and cleans protein sequences for downstream biophysical analysis."
outputs = ["cleaned_sequences.csv"]

[container]
image = "chiral-guardian:v1"

[scripts]
pre = "pre_run.sh"
run = "run.sh"
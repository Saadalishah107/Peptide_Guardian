name = "a_ingest"
description = "Phase 1: Harvest"
# Relative path inside the container
outputs = ["results/cleaned_sequences.csv"]

[container]
image = "chiral-guardian:v1"

[scripts]
pre = "pre_run.sh"
run = "run.sh"

